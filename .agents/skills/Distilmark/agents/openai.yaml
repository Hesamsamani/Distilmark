interface:
  display_name: "Distilmark"
  short_description: "Repo-specific patterns and workflows for Distilmark"
  default_prompt: "Use the Distilmark repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true